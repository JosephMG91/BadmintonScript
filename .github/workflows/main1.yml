# .github/workflows/run-from-cron.yml
name: Run from cron-job.org

on:
  workflow_dispatch:
    inputs:
      source:
        description: 'Trigger source'
        required: false

jobs:
  job:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Triggered via API"
